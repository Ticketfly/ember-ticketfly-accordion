import createAccordionAnimations from 'dummy/utils/create-accordion-animations';
import { module, test } from 'qunit';

module('Unit | Utility | create accordion animations');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = createAccordionAnimations();
  assert.ok(result);
});
